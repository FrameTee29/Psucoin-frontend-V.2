import styled from 'styled-components';

const StyledWrapper = styled.div`
  
`

const Signin = () => {
    return (
      <section>
        LOGIN
        
      </section>
    );
  }
  
  export default Signin;